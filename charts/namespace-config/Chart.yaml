apiVersion: v2
name: namespace-config
description: >
  Helm Library Chart für standardisierte Namespace-Konfiguration.
  Stellt Templates für Namespace, ResourceQuota, LimitRange,
  NetworkPolicy und RBAC bereit.
  Wird von jeder Team-App als Dependency eingebunden.
type: library
version: 1.0.0
