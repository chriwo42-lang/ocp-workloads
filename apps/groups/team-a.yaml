apiVersion: user.openshift.io/v1
kind: Group
metadata:
  name: team-a
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    # Team-A Mitglieder.
    # Rolle je App konfigurierbar via rbac.adminGroups / rbac.editGroups / rbac.viewGroups
users:
  - alice
