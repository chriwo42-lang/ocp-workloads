apiVersion: user.openshift.io/v1
kind: Group
metadata:
  name: project-b-viewers
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    # Zuweisung zu Namespaces erfolgt via namespace-config Chart (rbac.viewGroups)
    # Berechtigungen: view-RoleBinding in allen project-b-* Namespaces
    # ArgoCD: read-only Zugriff auf Apps in project-b (role: project-b-viewer)
users: []
# users:
#   - readonly-user
