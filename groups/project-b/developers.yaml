apiVersion: user.openshift.io/v1
kind: Group
metadata:
  name: project-b-developers
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    # Zuweisung zu Namespaces erfolgt via namespace-config Chart (rbac.editGroups)
    # Berechtigungen: edit-RoleBinding in allen project-b-* Namespaces
users:
  - developer
